{
  "name": "@donna/monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Donna multi-tenant utilities (tenancy, webhook-utils, messaging) monorepo",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm -ws run build",
    "dev": "npm -w examples/webhook-service run start:dev",
    "test": "npm -ws run test",
    "lint": "echo 'lint not configured'",
    "clean": "rimraf node_modules packages/*/node_modules examples/*/node_modules packages/*/dist examples/*/dist || true"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.5.0"
  }
}
